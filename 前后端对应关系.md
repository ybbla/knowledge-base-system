# 知识库系统 — 前端按钮与后端 API 对应关系

> 生成日期：2026-06-20  
> 状态：前端 22 个 API 方法 ↔ 后端 21 个 v1 端点，全部对上，零缺口。

---

## 一、架构总览

```
┌──────────────────────────────────────────────────────┐
│  前端 (frontend/)                                     │
│  index.html → js/api.js → js/components/             │
│    ├── app.js          应用入口 / 服务状态弹窗         │
│    ├── dashboard.js    仪表盘                         │
│    ├── documents.js    文档管理（列表/上传/CRUD）      │
│    ├── document-detail.js  文档详情（知识块/元素）     │
│    ├── search.js       知识搜索 + 检索调试            │
│    ├── chunks.js       知识块管理（列表/CRUD）         │
│    └── common.js       通用组件（Toast/Modal等）       │
└──────────────────────────────────────────────────────┘
                         ↕ HTTP
┌──────────────────────────────────────────────────────┐
│  后端 (knowledge_base_system/app/)                    │
│  api/v1/                                              │
│    ├── health.py      健康检查 (3 端点)                │
│    ├── documents.py   文档管理 (8 端点)                │
│    ├── chunks.py      知识块管理 (7 端点)              │
│    ├── search.py      检索 (3 端点)                   │
│    ├── services.py    索引同步/重建（支撑服务）         │
│    ├── schemas.py     统一响应模型                     │
│    └── errors.py      错误码/异常处理器                │
│                                                       │
│  api/ (旧版，全部 deprecated)                         │
│    ├── ingest.py      POST /ingest, GET /ingest/{id}  │
│    ├── search.py      POST /search                    │
│    ├── upload.py      POST /upload                    │
│    └── documents.py   GET /documents 等               │
└──────────────────────────────────────────────────────┘
```

---

## 二、仪表盘 `dashboard.js`

**页面路径：** `#/`

### 页面加载自动调用

| 触发 | 前端 API 调用 | 后端端点 | 后端文件:行 |
|---|---|---|---|
| 页面渲染 | `API.healthReady()` | `GET /api/v1/health/ready` | `health.py:48` |
| 页面渲染 | `API.healthDependencies()` | `GET /api/v1/health/dependencies` | `health.py:129` |
| 文档总数卡片 | `API.listDocuments({page:1,page_size:1})` | `GET /api/v1/documents` | `documents.py:144` |
| 知识块总数卡片 | `API.listChunks({page:1,page_size:1})` | `GET /api/v1/chunks` | `chunks.py:101` |
| 失败文档统计 | `API.listDocuments({page:1,page_size:1,status:'failed'})` | `GET /api/v1/documents` | `documents.py:144` |
| 处理中文档统计 | `API.listDocuments({page:1,page_size:1,status:'processing'})` | `GET /api/v1/documents` | `documents.py:144` |

### 按钮

| 按钮文案 | 位置 | 触发函数 | 后端端点 |
|---|---|---|---|
| ↑ 上传文档 | 快速操作区 | `Documents.showUploadModal()` | 纯前端弹窗 |
| ⌕ 搜索知识 | 快速操作区 | `App.router.navigate('/search')` | 纯前端路由跳转 |
| ▦ 浏览文档 | 快速操作区 | `App.router.navigate('/documents')` | 纯前端路由跳转 |
| ⊞ 知识块管理 | 快速操作区 | `App.router.navigate('/chunks')` | 纯前端路由跳转 |
| 失败文档 | 待处理事项 | `App.router.navigate('/documents')` | 纯前端路由跳转 |
| 处理中文档 | 待处理事项 | `App.router.navigate('/documents')` | 纯前端路由跳转 |

---

## 三、文档管理 `documents.js`

**页面路径：** `#/documents`

### 页面加载自动调用

| 触发 | 前端 API 调用 | 后端端点 | 后端文件:行 |
|---|---|---|---|
| `renderList()` | `API.searchFilters()` | `GET /api/v1/search/filters` | `search.py:352` |
| `loadPage()` | `API.listDocuments(params)` | `GET /api/v1/documents` | `documents.py:144` |

### 工具栏按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| ↑ 上传文档 | `showUploadModal()` | 纯前端弹窗 |
| 批量删除 | `batchDelete()` → `API.deleteDocument(id)` × N | `DELETE /api/v1/documents/{doc_id}` |
| 搜索框 <kbd>Enter</kbd> | `doSearch()` → `API.listDocuments(params)` | `GET /api/v1/documents` |
| 分类下拉 change | `doSearch()` → `API.listDocuments(params)` | `GET /api/v1/documents` |
| 状态下拉 change | `doSearch()` → `API.listDocuments(params)` | `GET /api/v1/documents` |
| 排序下拉 change | `doSearch()` → `API.listDocuments(params)` | `GET /api/v1/documents` |
| 清空筛选 | `resetFilters()` → `API.listDocuments(params)` | `GET /api/v1/documents` |
| ‹ 上一页 | `loadPage(page-1)` → `API.listDocuments(params)` | `GET /api/v1/documents` |
| 下一页 › | `loadPage(page+1)` → `API.listDocuments(params)` | `GET /api/v1/documents` |

### 表格每行按钮（active 状态文档）

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 文档标题 / 👁 详情 | `App.router.navigate(...)` | 纯前端路由跳转 |
| ✎ 编辑 | `showEditDialog(docId)` → 弹出编辑弹窗 | — |
| ↳ 编辑弹窗加载 | `API.getDocument(docId)` + `API.searchFilters()` | `GET /api/v1/documents/{doc_id}` |
| ↳ 保存修改 | `saveEdit(docId)` → `API.updateDocument(docId, {title, category})` | `PATCH /api/v1/documents/{doc_id}` |
| ↑ 更新 | `showUpdateModal(docId)` → 弹出更新弹窗 | — |
| ↳ ↑ 更新文档 | `doUpdateUpload()` → `API.uploadDocument(file, ...)` | `POST /api/v1/documents/upload` |
| 🗑 删除 | `deleteDoc(docId)` → `API.deleteDocument(docId)` | `DELETE /api/v1/documents/{doc_id}` |

### 表格每行按钮（deleted 状态文档）

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| ↶ 恢复 | `restoreDoc(docId)` → `API.restoreDocument(docId)` | `POST /api/v1/documents/{doc_id}/restore` |

### 上传弹窗按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 选择文件 | 浏览器 file picker | 纯前端 |
| ✕ (每个文件) | `removeSelectedFile(index)` | 纯前端 |
| ↑ 开始上传并入库 | `doUpload()` → `API.uploadDocument(file, title, category, {ingestAfterCreate:true})` | `POST /api/v1/documents/upload` |
| 取消 | `closeUploadModal()` | 纯前端 |

### 同名文件替换弹窗按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 取消 | `closeUploadModal()` | 纯前端 |
| 作为新文档上传 | `uploadAsNew(skipDocId)` | 纯前端 |
| 替换现有文档 | `confirmReplace(docId)` → 重新配置上传参数 | 纯前端（最终走 upload） |

---

## 四、文档详情 `document-detail.js`

**页面路径：** `#/documents/:id`

### 页面加载自动调用（并发请求 3 个接口）

| 触发 | 前端 API 调用 | 后端端点 | 后端文件:行 |
|---|---|---|---|
| `render(docId)` | `API.getDocument(docId)` | `GET /api/v1/documents/{doc_id}` | `documents.py:428` |
| 同上 | `API.listChunks({doc_id, page_size:200})` | `GET /api/v1/chunks` | `chunks.py:101` |
| 同上 | `API.listDocumentElements(docId, {page_size:500})` | `GET /api/v1/documents/{doc_id}/elements` | `documents.py:454` |

### 头部按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 编辑 | `Documents.showEditDialog(docId)` → 同文档列表编辑流程 | `GET` → `PATCH /api/v1/documents/{doc_id}` |
| 删除文档 | `Documents.deleteDoc(docId)` → `API.deleteDocument(docId)` | `DELETE /api/v1/documents/{doc_id}` |
| 恢复文档 | `Documents.restoreDoc(docId)` → `API.restoreDocument(docId)` | `POST /api/v1/documents/{doc_id}/restore` |

### 标签页切换

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 概览 / 知识块(N) / 解析元素(N) / 元数据 | `switchTab(tab)` | 纯前端 tab 切换（数据已加载） |
| 查看知识块（概览页内） | `switchTab('chunks')` | 纯前端 |
| 查看解析元素（概览页内） | `switchTab('elements')` | 纯前端 |

### 错误状态

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 重新加载 | `DocumentDetail.render(docId)` | 同上 3 个并发请求 |
| 返回文档列表 | `App.router.navigate('/documents')` | 纯前端路由跳转 |

---

## 五、知识搜索 `search.js`

**页面路径：** `#/search` 和 `#/search-debug`

### 知识搜索页面加载

| 触发 | 前端 API 调用 | 后端端点 | 后端文件:行 |
|---|---|---|---|
| `render()` | `API.searchFilters()` | `GET /api/v1/search/filters` | `search.py:352` |

### 知识搜索按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 搜索 / <kbd>Enter</kbd> | `doSearch()` → `API.search(query, topK, filters, options)` | `POST /api/v1/search` |
| 结果卡片点击 | `showResultDetail(chunkId)` | 纯前端（用 lastResult 在内存中的数据） |
| TopK「自定义...」 | `showCustomTopKDialog()` → 弹出输入框 | 纯前端 |
| ↳ 自定义 TopK「确认」 | `confirmCustomTopK()` | 纯前端 |

### 检索调试页面加载

| 触发 | 前端 API 调用 | 后端端点 | 后端文件:行 |
|---|---|---|---|
| `renderDebug()` | `API.searchFilters()` | `GET /api/v1/search/filters` | `search.py:352` |

### 检索调试按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 调试检索 / <kbd>Enter</kbd> | `doDebugSearch()` → `API.searchDebug(query, topK, filters)` | `POST /api/v1/search/debug` |

---

## 六、知识块管理 `chunks.js`

**页面路径：** `#/chunks`

### 页面加载自动调用

| 触发 | 前端 API 调用 | 后端端点 | 后端文件:行 |
|---|---|---|---|
| `render()` | `API.searchFilters()` | `GET /api/v1/search/filters` | `search.py:352` |
| 同上 | `API.listDocuments({page_size:200, status:'active'})` | `GET /api/v1/documents` | `documents.py:144` |
| `load()` | `API.listChunks(params)` | `GET /api/v1/chunks` | `chunks.py:101` |

### 工具栏按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 批量删除 | `batchDelete()` → `API.batchChunkOperation('delete', chunkIds)` | `POST /api/v1/chunks/batch` |
| + 新建知识块 | `showCreateDialog()` → 弹出弹窗 | — |
| ↳ 创建知识块（选已有文档） | `API.createChunk({doc_id, title, content, ...})` | `POST /api/v1/chunks` |
| ↳ 创建知识块（新建文档） | `API.createDocument(...)` + `API.createChunk(...)` | `POST /api/v1/documents` → `POST /api/v1/chunks` |
| 搜索框 <kbd>Enter</kbd> / 下拉 change | `load()` → `API.listChunks(params)` | `GET /api/v1/chunks` |
| 清空筛选 | `resetFilters()` → `load(1)` → `API.listChunks(params)` | `GET /api/v1/chunks` |
| ‹ 上一页 / 下一页 › | `load(page)` → `API.listChunks(params)` | `GET /api/v1/chunks` |

### 表格每行按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 标题 / 详情 | `showDetail(chunkId)` → `API.getChunk(chunkId)` | `GET /api/v1/chunks/{chunk_id}` |
| 删除 (active) | `deleteChunk(chunkId)` → `API.deleteChunk(chunkId)` | `DELETE /api/v1/chunks/{chunk_id}` |
| 恢复 (deleted) | `restoreChunk(chunkId)` → `API.restoreChunk(chunkId)` | `POST /api/v1/chunks/{chunk_id}/restore` |

### 抽屉内按钮

| 按钮文案 | 触发函数 | 后端端点 |
|---|---|---|
| 编辑 | `showEditDialog(chunkId)` → `API.getChunk(chunkId)` | `GET /api/v1/chunks/{chunk_id}` |
| ↳ 保存修改 | `updateChunkFromDialog(chunkId)` → `API.updateChunk(chunkId, {title, content, category, knowledge_type, reindex})` | `PATCH /api/v1/chunks/{chunk_id}` |
| 删除 (active) | `deleteChunk(chunkId)` → `API.deleteChunk(chunkId)` | `DELETE /api/v1/chunks/{chunk_id}` |
| 恢复 (deleted) | `restoreChunk(chunkId)` → `API.restoreChunk(chunkId)` | `POST /api/v1/chunks/{chunk_id}/restore` |

---

## 七、应用入口 `app.js`

| 触发 | 前端 API 调用 | 后端端点 | 后端文件:行 |
|---|---|---|---|
| 顶部「服务状态」点击 | `showServiceStatusDetail()` → `API.healthReady()` + `API.healthDependencies()` | `GET /api/v1/health/ready` + `GET /api/v1/health/dependencies` | `health.py:48` + `health.py:129` |
| 每 10 秒定时轮询 | `UI.ServiceStatus.check()` → `API.healthLive()` | `GET /api/v1/health/live` | `health.py:36` |

---

## 八、前端 `api.js` 方法 → 后端端点完整清单

| # | 前端方法 | HTTP | 后端端点 | 后端文件:行 | 状态 |
|---|---|---|---|---|---|
| 1 | `healthLive()` | GET | `/api/v1/health/live` | `health.py:36` | ✅ |
| 2 | `healthReady()` | GET | `/api/v1/health/ready` | `health.py:48` | ✅ |
| 3 | `healthDependencies()` | GET | `/api/v1/health/dependencies` | `health.py:129` | ✅ |
| 4 | `listDocuments(params)` | GET | `/api/v1/documents` | `documents.py:144` | ✅ |
| 5 | `createDocument(params)` | POST | `/api/v1/documents` | `documents.py:191` | ✅ |
| 6 | `getDocument(docId)` | GET | `/api/v1/documents/{doc_id}` | `documents.py:428` | ✅ |
| 7 | `listDocumentElements(docId)` | GET | `/api/v1/documents/{doc_id}/elements` | `documents.py:454` | ✅ |
| 8 | `updateDocument(docId, params)` | PATCH | `/api/v1/documents/{doc_id}` | `documents.py:456` | ✅ |
| 9 | `deleteDocument(docId)` | DELETE | `/api/v1/documents/{doc_id}` | `documents.py:499` | ✅ |
| 10 | `restoreDocument(docId)` | POST | `/api/v1/documents/{doc_id}/restore` | `documents.py:543` | ✅ |
| 11 | `uploadDocument(file, ...)` | POST | `/api/v1/documents/upload` | `documents.py:264` | ✅ |
| 12 | `getDocumentHistory(docId)` | GET | `/api/v1/documents/{doc_id}/history` | `documents.py:579` | ✅ |
| 13 | `listChunks(params)` | GET | `/api/v1/chunks` | `chunks.py:101` | ✅ |
| 14 | `createChunk(params)` | POST | `/api/v1/chunks` | `chunks.py:144` | ✅ |
| 15 | `getChunk(chunkId)` | GET | `/api/v1/chunks/{chunk_id}` | `chunks.py:195` | ✅ |
| 16 | `updateChunk(chunkId, params)` | PATCH | `/api/v1/chunks/{chunk_id}` | `chunks.py:211` | ✅ |
| 17 | `deleteChunk(chunkId)` | DELETE | `/api/v1/chunks/{chunk_id}` | `chunks.py:293` | ✅ |
| 18 | `restoreChunk(chunkId)` | POST | `/api/v1/chunks/{chunk_id}/restore` | `chunks.py:314` | ✅ |
| 19 | `batchChunkOperation(action, ids)` | POST | `/api/v1/chunks/batch` | `chunks.py:337` | ✅ |
| 20 | `search(query, topK, filters)` | POST | `/api/v1/search` | `search.py:218` | ✅ |
| 21 | `searchDebug(query, topK, filters)` | POST | `/api/v1/search/debug` | `search.py:276` | ✅ |
| 22 | `searchFilters()` | GET | `/api/v1/search/filters` | `search.py:352` | ✅ |

**前端 22 个方法 ↔ 后端 21 个 v1 端点：全部对应，零缺口。**

---

## 九、后端旧版路由（全部标记 deprecated，前端不调用）

| # | 端点 | 后端文件:行 | 废弃提示 |
|---|---|---|---|
| 1 | `POST /upload` | `upload.py:20` | Use `POST /api/v1/documents/upload` |
| 2 | `POST /ingest` | `ingest.py:29` | Use `/api/v1/documents/upload` |
| 3 | `GET /ingest/{job_id}` | `ingest.py:111` | Use `GET /api/v1/documents/{doc_id}` |
| 4 | `POST /search` | `search.py:15` | Use `POST /api/v1/search` |
| 5 | `GET /documents` | `documents.py:77` | Use `GET /api/v1/documents` |
| 6 | `GET /documents/{doc_id}` | `documents.py:95` | Use `GET /api/v1/documents` |
| 7 | `GET /documents/{doc_id}/elements` | `documents.py:108` | Use `GET /api/v1/documents` |
| 8 | `GET /documents/{doc_id}/chunks` | `documents.py:119` | Use `GET /api/v1/documents` |
